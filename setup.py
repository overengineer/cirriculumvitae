#!/usr/bin/python3
from setuptools import setup

setup(
    name='cv',
    version='1.0',
    long_description=__doc__,
    include_package_data=True,
    zip_safe=False,
    install_requires=['Flask']
)
